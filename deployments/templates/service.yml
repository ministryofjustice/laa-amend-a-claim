apiVersion: v1
kind: Service
metadata:
  name: laa-amend-a-claim
spec:
  selector:
    app: laa-amend-a-claim # this should match the pod label in deployment.yml
  ports:
    - name: https
      port: 8080
      targetPort: 8080