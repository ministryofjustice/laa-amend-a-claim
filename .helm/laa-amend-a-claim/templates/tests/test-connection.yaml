apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "laa-amend-a-claim.fullname" . }}-test-connection"
  labels:
    {{- include "laa-amend-a-claim.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "laa-amend-a-claim.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
